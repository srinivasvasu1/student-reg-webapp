apiVersion: v1
kind: Service
metadata:
  name: {{ include "studentreg-web-app.fullname" . }}
  labels:
    {{- include "studentreg-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "studentreg-web-app.selectorLabels" . | nindent 4 }}
